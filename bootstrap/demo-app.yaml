apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: demo-app
  namespace: argocd
spec:
  project: default
  source:
    repoURL: 'https://github.com/MdMaaz2003/healer-gitops.git'
    targetRevision: HEAD
    path: apps/demo-app
  destination:
    server: 'https://kubernetes.default.svc'
    namespace: app
  syncPolicy:
    automated:
      selfHeal: true
      prune: true
